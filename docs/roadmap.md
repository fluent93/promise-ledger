# Roadmap

## Now

- 01 Promise Ledger: Vercel-ready API MVP
- Validate hosted Groq extraction quality against real Korean/English chat snippets

## Next

- Run Groq model comparison against parser test cases
- Deploy 01 Promise Ledger to Vercel preview
- Introduce persistent DB after schema stabilizes
- Add calendar/reminder export

## Later

- Pick the second MVP candidate from the backlog
- Extract shared parser/schema helpers into `packages/shared-ai` if duplication appears
- Extract common UI patterns into `packages/shared-ui` if multiple apps become active
